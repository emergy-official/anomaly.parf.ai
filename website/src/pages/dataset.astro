---
import Layout from '~/layouts/PageLayout.astro';
import DatasetReact from '~/components/preact/DataseRreact';

const metadata = {
  title: 'Anomaly.parf.ai — Dataset explorer',
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}>
  <DatasetReact client:visible />
</Layout>
